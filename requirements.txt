# --- Core & Environment ---
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.66.0  # Progress bars for ingestion

# --- LLM Integration (The Brains) ---
groq>=0.5.0             # Main LLM Interface
google-generativeai>=0.5.0  # Gemini (For Swaraj's cleaning pipeline)

# --- Graph Database (Deep Brain) ---
neo4j>=5.20.0           # Official Python Driver

# --- Vector Database (Semantic Brain) ---
faiss-cpu>=1.8.0        # CPU version to ensure it runs everywhere
sentence-transformers>=2.7.0 # Free, local embeddings (HuggingFace)

# --- Keyword Search (Fast Brain) ---
rank_bm25>=0.2.2        # Simple, robust keyword retrieval

# --- UI & Deployment ---
chainlit>=1.1.0         # Chat Interface

# --- PDF Processing (For Swaraj) ---
pypdf>=4.2.0

pdfplumber
tqdm
regex
pillow
pytesseract

python-docx